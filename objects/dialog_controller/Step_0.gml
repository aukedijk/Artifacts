/// @description get dialog lines

// add player ability to make dialogg proceed forward
var next = false;
if ( keyboard_check_pressed( vk_anykey )) next = true;

//show_debug_message(fetch ? "fetch" : "not fetch");

//get_dialog( next );